name: "🚀 Init"
description: "프로젝트 초기 설정"
labels: ["init"]
body:
  - type: input
    id: jira_key
    attributes:
      label: 🎫 Jira 티켓 번호 (선택)
      placeholder: HOTSPOT-INIT
    validations:
      required: false

  - type: textarea
    attributes:
      label: 🏗️ 설정 내용
      description: 초기 설정 내용을 작성해주세요.
    validations:
      required: true